#include <stdio.h>
#include <stdbool.h>
#include <math.h>

#ifndef NUMCLASS_H
#define NUMCLASS_H

int isArmstrong(int);

int isPalindrome(int);

int isPrime(int);

int isStrong(int);

int factorial(int);

int countDigits(int);

int isArmstrongRecursive(int, int);



#endif